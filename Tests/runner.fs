#light

open System

(*let runner = new MbUnit.Core.AutoRunner()
runner.Run() |> ignore

if runner.Result.Counter.FailureCount > 0 then
    let n = Console.ReadLine()
    match n with
    | "r" -> runner.ReportToHtml()
    | _ -> ()
else
    System.Threading.Thread.Sleep(500)*)
(*using (var runner = new AutoRunner())
            {
                runner.Run();

                if (runner.Result.Counter.FailureCount > 0)
                {
                    var n = Console.ReadLine();
                    if (n == "r")
                    {
                        runner.ReportToHtml();
                    }
                }
                else
                {
                    Thread.Sleep(500);
                }
            }*)